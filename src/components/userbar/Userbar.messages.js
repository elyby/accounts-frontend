import { defineMessages } from 'react-intl';

export default defineMessages({
    register: {
        id: 'register',
        defaultMessage: 'Join'
    },

    logout: {
        id: 'logout',
        defaultMessage: 'Logout'
    }
});
